#include<bits/stdc++.h>
using namespace std;

void explainUSet(){
    unordered_set<int> st;
    /*
    - lower and upper bound function does not work,
    rest all functions are same.

    - it does not store in any particular order 

    - it has a better complexity than setin most cases, except when collision happens

    i.e. in worst case it goes O(n)[very rarely happens]
    */
}